# azure-web-app-laravel-sample

## Deploy Azure resources by Terraform

[Install Terraform](https://learn.hashicorp.com/terraform/azure/install), if you don't have already.

```ps
cd terraform/envs/dev
terraform init
terraform plan
terraform apply
```